server:
  servlet:
    context-path: /challenge

spring:
  datasource:
    url: jdbc:postgresql://database:5432/eventdb
    username: ehsan
    password: ehsan
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect


#  profiles:
#    active: dev

